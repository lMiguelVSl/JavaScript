

alert('Hola mundo');

let nombre = prompt('how is your name?');
console.log(nombre);
console.log('**' + nombre + '**');

const selection = confirm('Are you shure of that');
console.log(selection);

//this work into navegators because are part of the windows object but for example in Node dosen't work because node don't have the windows object

 